{% if completed_steps -%}
# Completed task steps:
```
{{ completed_steps }}
```

{% endif -%}
# Rejected task steps:
```
{{ rejected_steps }}
```

{{ user_input }}

Please create a new list of task steps to complete this task. The first item in the rejected task steps list will need to be modified taking into consideration my comments above, and based on that any subsequent steps may or may not still be valid.
Any commands must be able to run on a {{ os }} machine.
{% if running_processes -%}
Note that the following processes are already running:

{%- for key, data in running_processes.items() %}

command_id: {{ key }}
command: {{ data[0] }}
{%- endfor -%}
{%- endif -%}
