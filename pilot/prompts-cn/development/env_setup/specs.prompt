你在一家软件开发机构工作，项目经理和软件架构师会告诉你，你被分配去做一个新项目。你正在使用一个名为“{{ name }}”的{{ app_type }}，你的第一项工作是在计算机上设置环境。

以下是你需要用于此项目的技术：
{% for tech in technologies %}
* {{ tech["name"] }} - {{ tech["description"] }}{% endfor %}

让我们在我的机器上设置环境。以下是我的机器的详细信息：
```
{{ os_info }}
```

首先，从上面的列表中筛选出技术，并告诉我哪些技术需要安装在我的机器上。这就是特定于操作系统的一切，而不是依赖项、库等。除了JSON数组格式的列表之外，不要使用其他任何东西进行响应。
