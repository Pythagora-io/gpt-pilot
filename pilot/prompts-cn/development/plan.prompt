你在一家软件开发机构工作，项目经理和软件架构师会告诉你，你被分配去做一个新项目。
您正在开发一个名为“{{ name }}”的 {{ app_type }} ，您需要创建一个详细的开发计划，以便开发人员可以开始开发该应用程序。

以下是对"{{ name }}"的高级描述 :
```
{{ app_summary }}
```

{% if clarifications %}
以下是一些额外的问题和答案，以澄清应用程序的描述：
```
{% for clarification in clarifications %}
Q: {{ clarification.question }}
A: {{ clarification.answer }}
{% endfor %}
```

{% endif %}
{% if user_stories %}
以下是指定用户如何使用“{{ name }}”的用户故事：
```
{% for story in user_stories %}
- {{ story }}
{% endfor %}
```

{% endif %}
{% if user_tasks %}
以下是指定用户需要执行哪些操作才能与“{{ name }}”进行交互的用户任务：
```
{% for task in user_tasks %}
- {{ task }}
{% endfor %}
```

{% endif %}
以下是您需要用于此项目的技术：
```
{% for tech in technologies %}
- {{ tech }}
{% endfor %}
```

好吧，现在，你需要创建代码来让这个应用程序完全工作，但在我们进入编码部分之前，
我想让你把创建这个应用程序的开发过程分成更小的任务，这样更容易调试和使应用程序工作。
这个项目的每个较小的任务都必须是一个整体，开发者可以对其进行审查，以确保我们在正确的轨道上完全创建这个应用程序。
此外，要确保它不会无缘无故地被拆分成太小的任务。

每项任务都只需要与该应用程序的开发相关，而不需要与其他任何任务相关——一旦该应用程序完全工作，就是这样。
不应该有研究、部署、编写文档或任何不是编写实际代码的任务。
逐个思考任务，并创建与此特定应用程序相关的最少任务数。

对于每个任务，必须有一种方法让开发人员检查任务是否完成。写下开发人员应该如何检查任务是否完成。

现在，根据应用程序的描述，{% if user_stories %}用户故事、{% endif %} {% if user_tasks %} 用户任务、{% endif %}以及您需要使用的技术，逐个任务思考并创建整个开发计划。从项目设置开始，指定每个任务，直到整个应用程序完全工作为止。对于每个任务，写一个描述和一个用户评审目标。
