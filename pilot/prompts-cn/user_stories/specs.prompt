我希望你创建名为“{{ name }}”的{{ app_type }}，其描述如下：
```
{{ prompt }}
```

逐步思考{{ app_type }}“{{ name }}”的描述以及提供的问题和答案，并创建所有用户故事的列表。用户故事是对用户如何与{{ app_type }}进行交互的描述。例如，如果一个应用程序的描述是“创建一个脚本，查找频道名称中包含“测试”一词的Youtube频道”，则用户故事可能是：
-`user从CLI运行脚本`
-`user获取CSV文件中返回的频道列表`

**重要提示**：
一次输出一个用户故事。只返回一个用户故事。我可能会要求你修改一些用户故事。只有当我向你发送空回复时，你才能转到下一个用户故事。

**重要提示**：
创建完所有用户故事后，编写一个响应，其中只包含以下内容：
{{ END_RESPONSE }}
