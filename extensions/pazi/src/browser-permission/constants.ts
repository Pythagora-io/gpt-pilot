/**
 * Tool names that require browser permission to be enabled.
 * Used by the guard hook and the prompt hook.
 */
export const BROWSER_TOOL_NAMES = new Set(["browser", "web_search", "web_fetch", "browser_use"]);
