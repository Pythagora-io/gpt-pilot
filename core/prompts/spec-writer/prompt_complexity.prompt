{% if is_feature %}
Вот описание приложения, которое уже полностью создано:
```
{{ state.specification.description }}
```
Теперь я покажу вам описание функции, которую необходимо добавить в приложение:
{% endif %}
```
{{ prompt }}
```

{% if not is_fеature %}Выше приведен запрос пользователя для приложения/программного инструмента, который они пытаются разработать. {% endif %}Определите сложность запроса пользователя. НЕ отвечайте мыслями, рассуждениями, объяснениями или чем-либо подобным, верните ТОЛЬКО строковое представление уровня сложности. Используйте следующую шкалу:
"hard" для высокой сложности
"moderate" для средней сложности
"simple" для низкой сложности
